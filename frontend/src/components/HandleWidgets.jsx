import { Box } from "@mui/material";
import { styled } from "@mui/system";


export const HandleWidgets = styled(Box)(({ theme }) => ({
  padding: "1.5rem 1.5rem 0 1.75rem 1.5rem",
  backgroundColor: theme.palette.background.alt,
  borderRadius: "1rem",

}))

